{
  "name": "react-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist server.js",
    "lint": "tsc --noEmit",
    "migrate:sessions": "tsx scripts/migrate-sessions-firebase-to-supabase.ts",
    "migrate:squad": "tsx scripts/migrate-firestore-domain.ts squad",
    "migrate:attendance": "tsx scripts/migrate-firestore-domain.ts attendance",
    "migrate:exercises": "tsx scripts/migrate-firestore-domain.ts exercises",
    "migrate:exercise-deleted-ids": "tsx scripts/migrate-firestore-domain.ts exercise-deleted-ids",
    "migrate:fixtures": "tsx scripts/migrate-firestore-domain.ts fixtures",
    "migrate:video": "tsx scripts/migrate-firestore-domain.ts video",
    "migrate:physio": "tsx scripts/migrate-firestore-domain.ts physio",
    "migrate:team-logo": "tsx scripts/migrate-firestore-domain.ts team-logo",
    "migrate:session-cards": "tsx scripts/migrate-firestore-domain.ts session-cards",
    "migrate:permissions": "tsx scripts/migrate-firestore-domain.ts permissions",
    "migrate:all-domains": "npm run migrate:permissions && npm run migrate:squad && npm run migrate:attendance && npm run migrate:team-logo && npm run migrate:exercises && npm run migrate:exercise-deleted-ids && npm run migrate:session-cards && npm run migrate:fixtures && npm run migrate:video && npm run migrate:physio"
  },
  "dependencies": {
    "@google/genai": "^2.4.0",
    "@supabase/supabase-js": "^2.112.2",
    "@tailwindcss/vite": "^4.1.14",
    "@vitejs/plugin-react": "^5.0.4",
    "dotenv": "^17.2.3",
    "express": "^4.21.2",
    "firebase": "^12.16.0",
    "heic2any": "^0.0.4",
    "lucide-react": "^0.546.0",
    "motion": "^12.23.24",
    "react": "^19.0.1",
    "react-dom": "^19.0.1",
    "recharts": "^3.10.1",
    "vite": "^6.2.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.14.0",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "autoprefixer": "^10.4.21",
    "esbuild": "^0.25.0",
    "tailwindcss": "^4.1.14",
    "tsx": "^4.21.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.3"
  }
}
